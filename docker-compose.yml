services:
  api:
    image: ghcr.io/jives00/travel-api:latest
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: travel-api
    ports:
      - "3008:3008"
    env_file: .env
    environment:
      # Uploaded photo-album thumbnails (trip_links). Inside the bind mount
      # below, so they survive a redeploy — the app defaults to this path, but
      # naming it here keeps the coupling to the volume visible.
      UPLOAD_DIR: /app/apps/api/uploads/link-images
    volumes:
      - /volume2/docker/travel/uploads:/app/apps/api/uploads
    networks: [shared-db]
    restart: unless-stopped

  web:
    image: ghcr.io/jives00/travel-web:latest
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    container_name: travel-web
    ports:
      - "3003:3000"
    environment:
      API_URL: http://travel-api:3008
    networks: [shared-db]
    restart: unless-stopped

networks:
  shared-db:
    external: true
