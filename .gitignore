.claude/
plans/
.env
.env.local

# deps / build output
node_modules/
dist/
.next/
.expo/
*.tsbuildinfo

# android
apps/mobile/android/
*.keystore

# misc
.DS_Store

# Uploaded link thumbnails (UPLOAD_DIR) — bind-mounted in the container,
# a local folder in dev. Never committed.
apps/api/uploads/
